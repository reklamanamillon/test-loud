name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy to Server
        run: |
          sshpass -p '  ' ssh kornilov@158.160.137.62 "cd /var/www/html && git pull origin main"
